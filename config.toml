# 麦麦自主规划插件配置文件
#
# ℹ️ 功能说明：与本体规划系统的关系
# ==================================================
# MaiBot 有两层规划系统：
#
# 1. planner_actions（核心本体）：基础规划框架
#    - 提供规划和动作执行的基础设施
#    - 用于实时对话中的动作规划
#
# 2. autonomous_planning_plugin（本插件）：高级自主规划
#    - 基于核心规划框架，提供更高级的功能
#    - 长期目标管理（创建、暂停、完成、取消）
#    - 自动生成每日/每周/每月日程（LLM智能生成）
#    - 定时任务调度（健康检查、社交维护、学习计划）
#
# 功能定位：
# - 核心规划系统：短期、实时规划（对话中的动作）
# - 本插件：长期、自主规划（日程管理、定时任务）
#
# ⚠️ 重要：本插件调用核心API，不会产生冲突
# 本插件通过标准的 Tool、Action、EventHandler 机制工作，
# 会自动与核心规划系统协调，不会产生冲突。
#
# 🆕 v2.2.0 更新：集成到 Planner 系统
# - 新增 SelfPlanningAction，让麦麦可以通过 Planner 主动规划日程
# - Planner 会根据上下文智能判断是否需要执行规划动作
# - 移除了独立的自动日程生成循环，改为 Action 驱动
#
# 当前配置：已启用，提供完整的自主规划功能
# ==================================================

[plugin]
# 是否启用自主规划插件
# ✅ 建议启用：提供强大的自主规划和日程管理功能
enabled = true

[autonomous_planning]
# 🆕 目标清理间隔（秒），默认1小时
# EventHandler 会定期清理已完成/已取消的旧目标
cleanup_interval = 3600

# 清理多少天前的已完成/已取消目标
cleanup_old_goals_days = 30

# 目标类型配置示例（可选）
# 以下配置会在创建对应类型目标时作为默认参数

[autonomous_planning.goal_types.health_check]
# 健康检查目标的默认配置
check_device = true
check_plugins = false
report_format = "summary"

[autonomous_planning.goal_types.social_maintenance]
# 社交维护目标的默认配置
inactive_threshold_hours = 24
greeting_type = "friendly"

[autonomous_planning.goal_types.learn_topic]
# 学习目标的默认配置
depth = "intermediate"
topics = ["Python", "AI"]

# ===== 日程生成配置 =====
# 🆕 v2.2.0: 现在日程生成由 SelfPlanningAction 驱动
# Planner 会根据上下文智能判断何时执行自我规划
# 不再需要配置自动生成时间，Planner 会自主决策

[autonomous_planning.schedule]
# 是否自动注入日程到回复的prompt
# true: 麦麦会在回复时自然提到当前活动（推荐）
# false: 不注入日程信息
inject_schedule = true

[autonomous_planning.schedule.preferences]
# 作息时间
wake_up_time = "07:30"
sleep_time = "23:30"

# 三餐时间
breakfast_time = "08:00"
lunch_time = "12:00"
dinner_time = "18:00"

# 学习相关
has_classes = true
class_time_morning = "09:00"
class_time_afternoon = "14:00"
study_time = "21:00"

# 娱乐爱好
entertainment_time = "19:00"
favorite_activities = ["刷贴吧", "看动漫", "玩游戏"]

# 兴趣学习
learning_topics = ["Python编程", "游戏开发", "动漫文化"]

# 社交
social_active = true

# 运动
exercise_occasionally = true

# 注意：以下旧配置已弃用（v2.2.0）
# auto_generate_daily, daily_trigger_time 等配置不再使用
# 现在由 SelfPlanningAction 和 Planner 智能决策何时生成日程
